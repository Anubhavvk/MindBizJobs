import React from 'react';
import MainContent from '../components/MainContent'; // Path to MainContent component

const Home = () => {
  return (
    <div>
      <MainContent />
    </div>
  );
};

export default Home;
